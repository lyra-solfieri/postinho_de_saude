import 'package:flutter/material.dart';

class LocalizacaoPostinho extends StatelessWidget {
  const LocalizacaoPostinho({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Localização do seu Postinho"),
      ),
    );
  }
}
